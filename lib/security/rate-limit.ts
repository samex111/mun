/**
 * Rate limiting scaffold — always passes in Phase 4.
 * Replaced with real implementation in a future phase.
 */
export async function rateLimit(_identifier: string): Promise<{ success: true }> {
  return { success: true };
}
